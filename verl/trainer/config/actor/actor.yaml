# Format checks enforced on CI:
# 1. Comments must appear above each field.
# 2. There must be a blank line between each field.
# 3. Inline comments (after a field on the same line) are not allowed.
# 4. Indentation level is respected for nested fields.

# Target class for this configuration
_target_: verl.workers.config.ActorConfig

# Number of rollouts per update (mirrors actor rollout_n)
rollout_n: ${oc.select:actor_rollout_ref.rollout.n,1}

# the abstract actor configs
# fsdp, fsdp2 or megatron. must be set.
strategy: ???

# Split each sample into sub-batches of this size for PPO
ppo_mini_batch_size: 256

# [Deprecated] Global micro batch size
ppo_micro_batch_size: null

# Local per-GPU micro batch size
ppo_micro_batch_size_per_gpu: null

# Whether to automatically adjust batch size at runtime
# oc.select: the default val for ref.log_prob_use_dynamic_bsz
use_dynamic_bsz: false

# Max tokens per GPU in one PPO batch; affects gradient accumulation
# Typically it should be: n * ${data.max_prompt_length} + ${data.max_response_length}
# oc.select: the default val for ref.log_prob_max_token_len_per_gpu
ppo_max_token_len_per_gpu: 16384

# PPO clip ratio
clip_ratio: 0.2

# Lower bound for asymmetric clipping (used in dual-clip PPO)
clip_ratio_low: 0.2

# Upper bound for asymmetric clipping (used in dual-clip PPO)
clip_ratio_high: 0.2

# Positive and negative tau for smoothing function in SAPO (https://arxiv.org/pdf/2511.20347)
# default values used in the paper with Qwen3-30B-A3B-Base
tau_pos: 1.0

# negative tau for smoothing function in SAPO
tau_neg: 1.05

# Whether to freeze vision model, if set true, it will be freeze vision model
freeze_vision_tower: false

# policy loss config
policy_loss:

  # # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
  _target_: verl.workers.config.PolicyLossConfig

  # Loss function mode: vanilla / clip-cov / kl-cov /gpg from https://arxiv.org/abs/2505.22617
  loss_mode: "vanilla"

  # Ratio of tokens to be clipped for clip-cov loss
  clip_cov_ratio: 0.0002

  # Lower bound for clip-cov loss
  clip_cov_lb: 1.0

  # Upper bound for clip-cov loss
  clip_cov_ub: 5.0

  # Ratio of tokens to be applied kl penalty for kl-cov loss
  kl_cov_ratio: 0.0002

  # KL divergence penalty coefficient
  ppo_kl_coef: 0.1

# Constant C in Dual-clip PPO; clips when advantage < 0 and ratio > C
clip_ratio_c: 3.0

# Loss aggregation mode: "token-mean", "seq-mean-token-sum", "seq-mean-token-mean", or "seq-mean-token-sum-norm"
loss_agg_mode: token-mean

# Scale factor for "seq-mean-token-sum-norm" loss aggregation mode.
# If null, uses response_length. Set to a constant to ensure consistent normalization.
loss_scale_factor: null

# Entropy regularization coefficient in PPO loss
entropy_coeff: 0

# When true, the actor forward will request entropy from the model
calculate_entropy: false

# This computes Σπ² needed for the Logit-Gradient Norm proxy W(τ) = Σ_t[1 - 2π_t + Σπ²]
# c.f. https://yingru.notion.site/The-Optimal-Token-Baseline-399211a558b782cfa936014c0d42dfb8
calculate_sum_pi_squared: false

# Whether to use KL loss instead of KL reward penalty. True for GRPO
use_kl_loss: false

# Whether to enable PrefixGrouper shared-prefix forward
use_prefix_grouper: false

# Whether to use torch.compile()
# oc.select: the default val for ref.use_torch_compile
use_torch_compile: true

# KL loss coefficient when use_kl_loss is enabled. For GRPO
kl_loss_coef: 0.001

# Type of KL divergence loss. Options: "kl"(k1), "abs", "mse"(k2), "low_var_kl"(k3), "full"
kl_loss_type: low_var_kl

# contextdistillation fork (M4): independent straight-through gradient scales for the
# teacher / student path of the local full-vocab forward-KL(teacher‖student). Value
# unchanged; these scale only the gradient and also carry the KL weight β (a global
# coefficient folds into both, so there is no separate kl_local_coef). Only used when
# model.model_type == teacher_student_language_model. Both 0.0 => KL gradient frozen.
kl_teacher_grad_scale: 1.0

kl_student_grad_scale: 1.0

# contextdistillation fork: gate the local-KL STUDENT path on per-response boxed correctness
# (only distill toward the student for correct rollouts). false => uniform student scale.
kl_student_gate_correct: false

# contextdistillation fork (issue #24): hard-CE gate for the local-KL STUDENT path. When true,
# the student-path gradient is the hard cross-entropy on the SAMPLED token −∇log π_s(y_t)
# instead of the full-vocab KL gradient −Σ_a π_t(a)·∇log π_s(a). Logged KL value + teacher path
# stay full-vocab; only the student gradient changes. false => full-vocab student KL gradient.
kl_student_gate_sampled: false

# contextdistillation fork (M6): straight-through grad scale on the teacher (privileged) path
# of the ref-anchor KL(π_θ(·|x,z) ‖ π_ref(·|x)), carrying the weight γ. The ref (θ₀) argument is
# a frozen detached constant. Default 0.0 => term OFF (engine skips the ref forward). Non-zero
# => the colocated ref worker is forced on and reused for a live forward on the student sequence.
kl_ref_grad_scale: 0.0

# contextdistillation fork (issue #24): which context the frozen ref (θ₀) is fed for the ref-anchor
# KL. "student" (default) => ref sees the unprivileged prompt π_ref(·|x) (KL(teacher‖frozen student));
# "teacher" => ref sees the privileged prompt π_ref(·|x,z) (KL(teacher‖frozen teacher), a trust-region
# anchor on the privileged policy). Only used when kl_ref_grad_scale != 0.
kl_ref_context: student

# contextdistillation fork (issue #38, SDPO/SDFT): EMA the reference model instead of freezing θ₀.
# After each actor step the ref tracks the actor: θ_ref <- (1-ema_ref)·θ_ref + ema_ref·θ_actor
# (SDPO update-rate convention: ema_ref is the RATE, LOW = slower drift). ema_ref=0.0 (default)
# => frozen θ₀ (byte-for-byte pre-#38); 1.0 => copy actor each step. Only meaningful with
# kl_ref_grad_scale != 0 (the M6 ref consumer).
ema_ref: 0.0

# contextdistillation fork (M7 / issue #32): the SECOND Tang–Munos term of ∇KL, the global
# (score-function) term Σ_t ∇logπ_θ(y_t|x,z,y_<t)·Σ_{s>t} KL_s. When on, the teacher_student loss
# folds a per-token KL reward-to-go into the PPO advantage so verl's clipped-IS-ratio PG produces
# it; it reuses each KL's teacher-path coefficient (kl_teacher_grad_scale / kl_ref_grad_scale) — no
# separate coefficient. global_kl_term ∈ {null, "sum", "mean"}: null => OFF (advantage untouched,
# M4/M6); "sum" => G_t = Σ_{s>t} KL_s; "mean" => each G_t divided by the NUMBER OF TERMS in its OWN
# summation Σ_{s>t} (the count of future tokens H-1-t at position t) — i.e. the mean future-KL
# contribution per remaining token, NOT G_t divided by a constant sequence length. The divisor
# shrinks as t advances ([H-1,…,1,0]); this removes the length bias of "sum" (long responses
# accumulate a larger G_t purely from more terms, driving the collapse to empty responses).
# Composes with global_kl_baseline.
global_kl_term: null

# contextdistillation fork (M8): per-timestep baseline (variance reduction) for the global term.
# null => raw REINFORCE fold in the loss (M7). An estimator name (e.g. "rloo"/"grpo", mapped to the
# vectorized variants) => subtract a per-(uid,t) leave-one-out / group-relative baseline computed at
# the advantage stage (requires a forward-only KL precompute pass). Only used when global_kl_term is set.
global_kl_baseline: null

# contextdistillation fork (M8): per-GPU token budget for the forward-only KL precompute pass. null =>
# ppo_max_token_len_per_gpu (the training budget). That pass is no_grad/no-activations but holds 2-3 full-vocab
# logit tensors at once, so its safe ceiling is below the single-forward log-prob infer budget
# (rollout.log_prob_max_token_len_per_gpu). Raise it to cut the precompute's micro-batch count. Only used when
# global_kl_baseline is set.
global_kl_max_token_len_per_gpu: null

# contextdistillation fork (issue #41): per-token KL hinge threshold (nats) applied to the GLOBAL
# (teacher policy-gradient / score-function) term ONLY. The reward-to-go becomes G_t = Σ_{s>t}
# max(0, KL_s − kl_global_threshold), gating both the local (kl_teacher_grad_scale) and ref
# (kl_ref_grad_scale) global folds — the direct local/ref KL losses stay RAW. 0.0 (default) => no
# hinge, byte-for-byte pre-#41. Only has effect when global_kl_term is set (M7 loss-side fold or
# M8 baselined trainer fold). Shared threshold across both global terms.
kl_global_threshold: 0.0

# contextdistillation fork (AGRO, issue #43): fraction of each prompt's n TRAIN rollouts drawn from
# the UNPRIVILEGED student prompt; the rest from the privileged teacher prompt. Realizes the mixture
# sampler mu = sg(1/2 pi(.|x) + 1/2 pi(.|x,z)) (default 0.5). Endpoints allowed: 0.0 => all-teacher,
# 1.0 => all-student. Interior ratios must split n into whole student/teacher halves (asserted). Only
# the ROLLOUT SOURCE; the AGRO loss uses teacher/student/ref logprobs for every rollout regardless of
# source (the source is a diagnostics-only tag). Only used by the AGRO engine + loss.
student_rollout_ratio: 0.5

# contextdistillation fork (off-policy GRPO length-collapse debug, issue #48): how the per-sequence
# KL-to-ref term inside R_beta = scores - beta*KL is aggregated over response tokens before the
# source-split baseline. NAMED knob (extensible; Jason 2026-07-28), mirroring global_kl_term:
#   "sum"  (default) => R_beta = sum_t scores_t - beta * sum_t (lp_ctx_t - lp_ref_t) (pre-#48).
#   "mean"           => the KL term is divided by |y| (response length): only the KL term is
#                      length-normalized, the outcome reward sum_t scores_t is untouched. Removes the
#                      length-scaling of the KL penalty (suspected driver of bare-\boxed{} collapse).
# Only used by the off-policy GRPO trainer (model_type == offpolicy_grpo_language_model).
kl_reward_norm: sum

# contextdistillation fork (off-policy IS-ratio rework, issue #48): how the per-token IS ratio
# rho = pi_ctx(theta) / pi_behavior_old is bounded before multiplying the detached advantage.
# pi_behavior is the policy that SAMPLED the rollout (student-context for student-sourced,
# teacher-context for teacher-sourced), so a source-MATCHED path is on-policy (rho==1) and only the
# cross path carries an IS correction. Band = [1-clip_ratio_low, 1+clip_ratio_high] (PPO knobs).
#   "clip"     (default) => standard PPO pessimistic surrogate via compute_policy_loss_vanilla.
#   "zero_adv"           => zero the advantage (zero gradient) for tokens with rho OUTSIDE the band
#                           (both directions); in-band tokens get the raw rho*A score.
# Only used by the off-policy GRPO trainer (model_type == offpolicy_grpo_language_model).
offpolicy_is_clip_mode: clip

# Number of PPO epochs per batch
ppo_epochs: 1

# Shuffle training data across PPO epochs
shuffle: false

# The seed used to construct mini-batch
data_loader_seed: 42

# checkpoint configs
checkpoint:

  # Target dataclass for this configuration
  _target_: verl.trainer.config.CheckpointConfig

  # What to include in saved checkpoints
  # with 'hf_model' you can save whole model as hf format, now only use sharded model checkpoint to save space
  save_contents: ['model', 'optimizer', 'extra']

  # For more flexibility, you can specify the contents to load from the checkpoint.
  # .xxx refers to the local variable xxx from the same level of hierarchy similar to python pkg
  load_contents: ${.save_contents}

  # Whether to save checkpoints asynchronously. Only effective for Megatron as of now.
  async_save: False

  # Whether to perform strict validation during weight export
  strict: True

# optimizer configs
optim:

  # Learning rate
  lr: 1e-6

  # Warmup steps ratio (used if lr_warmup_steps is 0 or negative)
  lr_warmup_steps_ratio: 0.0

  # Total training steps (must be overridden at runtime)
  total_training_steps: -1

  # Weight decay
  weight_decay: 0.01

  # Prioritized. None, 0 or Negative values mean delegating to lr_warmup_steps_ratio.
  lr_warmup_steps: -1


# Whether to use custom fused kernels (e.g., FlashAttention, fused MLP)
use_fused_kernels: ${oc.select:actor_rollout_ref.model.use_fused_kernels,false}

# profile the actor model in `update_policy` 
profiler:

  # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
  _target_: verl.utils.profiler.ProfilerConfig

  # profiler tool, default same as profiler.tool in global config
  # choices: nsys, npu, torch
  tool: ${oc.select:global_profiler.tool,null}

  # whether enable profile on Actor
  enable: False
  
  # Whether to profile all ranks.
  all_ranks: False

  # The ranks that will be profiled. [] or [0,1,...]
  ranks: []

  # profile results saving path
  save_path: ${oc.select:global_profiler.save_path,null}

  # specific tool config which only related to the role
  tool_config:

    # nsys tool config
    nsys:

      # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
      _target_: verl.utils.profiler.config.NsightToolConfig
    
      # True for each task has its own database, False for all tasks in one training step share one database.
      discrete: ${oc.select:global_profiler.global_tool_config.nsys.discrete}
    
    # npu config
    npu:

      # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
      _target_: verl.utils.profiler.config.NPUToolConfig

      # Contents to profile, can be empty
      # options: npu, cpu, memory, shapes, module, stack
      contents: []

      # Collection level, optional values: level_none, level0, level1, level2.
      level: "level0"

      # Whether to automatically parse the data.
      analysis: True

      # True for each task has its own database, False for all tasks in one training step share one database.
      discrete: False
    
    # torch profiler config
    torch:

      # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
      _target_: verl.utils.profiler.config.TorchProfilerToolConfig

      # Contents to profile, can be empty
      # options: cuda, cpu, memory, shapes, stack
      contents: []

      # True for each task has its own database, False for all tasks in one training step share one database.
      discrete: false

    # torch memory profiler config
    torch_memory:

      # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
      _target_: verl.utils.profiler.config.TorchMemoryToolConfig

      # Maximum number of memory allocation entries to track
      trace_alloc_max_entries: ${oc.select:global_profiler.global_tool_config.torch_memory.trace_alloc_max_entries,100000}

      # Stack trace depth for memory allocations
      stack_depth: ${oc.select:global_profiler.global_tool_config.torch_memory.stack_depth,32}

    # msprobe precision debugger config
    precision_debugger:

      # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
      _target_: verl.utils.profiler.config.PrecisionDebuggerToolConfig

      # Path to msprobe config json
      config_path: ${oc.select:global_profiler.global_tool_config.precision_debugger.config_path,null}

      # Deprecated and ignored. Use global_profiler.steps.
      steps: null

      # Profile stages
      # Supported stages: actor_update, actor_compute_log_prob, ref_compute_log_prob,
      # compute_values, critic_update, compute_rm_score
      stages: ${oc.select:global_profiler.global_tool_config.precision_debugger.stages,null}

      # Whether to fail on unknown stage or missing msprobe
      strict: ${oc.select:global_profiler.global_tool_config.precision_debugger.strict,False}

# Router replay configuration for MoE models
router_replay:

  # Target dataclass for this configuration
  _target_: verl.workers.config.RouterReplayConfig

  # Router replay mode: disabled, R2, R3
  # - R2: Use R2 routing strategy (record mode)
  # - R3: Use R3 routing strategy (record mode)
  mode: disabled

  # File path to save recorded routing decisions
  # Required when mode is 'record', 'R2', or 'R3'
  record_file: null

  # File path to load recorded routing decisions for replay
  # Required when mode is 'replay'
  replay_file: null

# QAT (Quantization-Aware Training) configuration
# When enabled:
#   - QAT is automatically applied to actor model during training
#   - Fused scales (QKV/GateUp) are automatically enabled for training-inference consistency
#   - Fast quantization is used when syncing weights to vLLM rollout
# Supported modes: "w4a16" (NVFP4 weight-only)
# Note: "w4a4" mode is included in the code but currently has KL divergence issues and is NOT recommended for use.
# For usage examples, see: https://github.com/verl-project/verl-recipe/blob/main/qat/README.md
qat:

  # Whether to enable QAT
  enable: false

  # Quantization mode: "w4a16" (weight-only). "w4a4" is experimental and not recommended.
  mode: "w4a16"

  # Quantization group size (NVFP4 requires 16)
  group_size: 16

  # Patterns to ignore (e.g., lm_head, embed_tokens)
  ignore_patterns:

    - "lm_head"
    - "embed_tokens"
    - "re:.*mlp.gate$"

  # Activation observer for W4A4 mode: "static_minmax", "memoryless_minmax", or "minmax"
  activation_observer: "static_minmax"

  # Path to vLLM quantization config JSON file
  quantization_config_path: null

